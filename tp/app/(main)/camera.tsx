import CameraScreen from "../../components/camera-screen/CameraScreen";
import {SafeAreaView} from "react-native-safe-area-context";

export default function CameraPage() {
    return (<SafeAreaView>
            <CameraScreen/>
        </SafeAreaView>
    )
};